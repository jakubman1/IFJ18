PROG → def id PARAMS eol PROG2 end eol PROG
PROG → STATEMENT eol PROG
PROG → ε
PROG2 → STATEMENT eol PROG
PROG2 → ε 
PARAMS → ( PARAMS )
PARAMS → id PARAMS_N
PARAMS → ε
PARAMS_N → , id PARAMS_N
PARAMS_N → ε
STATEMENT → if EXPRESSION then eol PROG2 else eol PROG2 end
STATEMENT → while EXPRESSION do eol PROG2 end
STATEMENT → id DEF_ARGS
STATEMENT → ε
DEF_ARGS → = DEFINE
DEF_ARGS → ARGS
DEFINE → id ARGS
DEFINE → print ARGS
DEFINE → inputs ARGS
DEFINE → inputi ARGS
DEFINE → inputf ARGS
DEFINE → lenght ARGS
DEFINE → substr ARGS
DEFINE → ord ARGS
DEFINE → chr ARGS
DEFINE → EXPRESSION
DEFINE → ε
ARGS → ε
ARGS → ( ARGS )
ARGS → VALUE ARGS_N
ARGS_N → , VALUE ARGS_N
ARGS_N → ε
VALUE → int
VALUE → float
VALUE → id
VALUE → string
EXPRESSION → int	// pouze pomocny jako nahrada za prec_table

FACTORIAL.RB:
eol id = int eol def id ( id ) eol if int then eol id = int eol else eol id = int eol id = id id eol id = int eol end eol id eol end eol		// do sem ok -- to je blok pred tim velkym komentem
eol eol print string eol id = inputi eol if int then eol print string eol else eol id = id id eol print ( string ) eol end eol 					// tady uz zacinaj vestaveny funkce a tam se to sere
